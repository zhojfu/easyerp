<UserControl x:Class="EasyERP.Desktop.Stock.StockManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:viewModels="clr-namespace:EasyERP.Desktop.ViewModels"
             xmlns:stock="clr-namespace:EasyERP.Desktop.Stock"
             d:DataContext="{d:DesignInstance Type=stock:StockManagerViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="20,0,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Height="50" VerticalAlignment="Top">
            <TextBlock Text="Manage Projects" Width="100" HorizontalAlignment="Left" VerticalAlignment="Center" />
            <StackPanel Margin="0,0,0,0" Orientation="Horizontal" FlowDirection="RightToLeft"
                        HorizontalAlignment="Right" Width="680">
                <StackPanel.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.MergedDictionaries>
                            <ResourceDictionary
                                Source="pack://application:,,,/MahApps.Metro;component/Styles/FlatButton.xaml" />
                        </ResourceDictionary.MergedDictionaries>
                    </ResourceDictionary>
                </StackPanel.Resources>
                <Button Margin="10,0,0,0" Width="60" Height="30" Content="Delete" cal:Message.Attach="Delete" />
                <Button Margin="10,0,0,0" Width="60" Height="30" Content="Export" cal:Message.Attach="Export" />
                <Button Margin="10,0,0,0" Width="60" Height="30" Content="Import" cal:Message.Attach="Import" />
                <Button Margin="10,0,0,0" Width="60" Height="30" Content="Add" x:Name="Add" />
            </StackPanel>
        </Grid>

        <GridSplitter Grid.Row="1" Height="3" Background="Azure" />
        <StackPanel Grid.Row="2">
            <telerik:RadGridView ItemsSource="{Binding StockList }" AutoGenerateColumns="False"
                                 SelectionMode="Extended"
                                 ShowGroupPanel="False" Margin="0,10,0,0" IsReadOnly="True">
                <telerik:RadGridView.Columns>
                    <telerik:GridViewDataColumn Header="Product Name" DataMemberBinding="{Binding Path=ProductName}"
                                                Width="120" />
                    <telerik:GridViewDataColumn Header="ProductionTime"
                                                DataMemberBinding="{Binding Path=ProductionTime}" Width="120" />
                    <telerik:GridViewDataColumn Header="Quantity" DataMemberBinding="{Binding Path=Quantity}"
                                                Width="80" />
                    <telerik:GridViewDataColumn Header="Cost" DataMemberBinding="{Binding Path=Cost}" Width="80" />
                    <telerik:GridViewDataColumn Header="Origin" DataMemberBinding="{Binding Path=Origin}" Width="120" />
                </telerik:RadGridView.Columns>
            </telerik:RadGridView>
        </StackPanel>
    </Grid>
</UserControl>