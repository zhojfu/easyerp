namespace EasyERP.Web.Validators.Employees
{
    //using EasyERP.Web.Framework.Validators;
    //using EasyERP.Web.Models.Employee;
    //using FluentValidation;

    //public class EmployeeValidator : BaseValidator<EmployeeModel>
    //{
    //    public EmployeeValidator()
    //    {
    //        RuleFor(e => e.FirstName).Length(1, 10).WithMessage("名不能为空, 且不能超过10个字符");
    //        RuleFor(e => e.LastName).Length(1, 10).WithMessage("姓不能为空, 且不能超过10个字符");
    //        RuleFor(e => e.IdNumber).Matches(@"^\d{18}[xX]{0,1}$").WithMessage("身份证不符合规则");
    //    }
    //}
}