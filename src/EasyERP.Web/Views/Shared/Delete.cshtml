@model EasyERP.Web.Framework.Mvc.DeleteConfirmationModel
@using (Html.BeginForm(Model.ActionName, Model.ControllerName, new { id = Model.Id }))
{
    @Html.AntiForgeryToken()
    <div>
        <p>
            DeleteConfirmation
        </p>
        <div class="form-actions">
            <span id="" class="k-button" onclick="$('#@Model.WindowId').data('kendoWindow').close();">Cancel</span>
            <input type="submit" class="k-button" value="Delete" />
        </div>
    </div>
}